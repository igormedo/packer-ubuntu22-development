# Created by https://www.toptal.com/developers/gitignore/api/packer,vagrant,ansible
# Edit at https://www.toptal.com/developers/gitignore?templates=packer,vagrant,ansible

### Ansible ###
*.retry

### Packer ###
# Cache objects
packer_cache/
iso/
build/

# Crash log
crash.log

# https://www.packer.io/guides/hcl/variables
# Exclude all .pkrvars.hcl files, which are likely to contain sensitive data,
# such as password, private keys, and other secrets. These should not be part of
# version control as they are data points which are potentially sensitive and
# subject to change depending on the environment.
#
*.pkrvars.hcl

# For built boxes
*.box

### Packer Patch ###
# ignore temporary output files
output-*/

### Vagrant ###
# General
.vagrant/

# Log files (if you are creating logs in debug mode, uncomment this)
*.log

### Vagrant Patch ###

# End of https://www.toptal.com/developers/gitignore/api/packer,vagrant,ansible